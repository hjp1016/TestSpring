language: java
jdk:
  - openjdk8

branches:
  only:
    - master

before_script:
  - cd {TestSpring}/
# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

before_deploy:
  - zip -r StudyProject-springboot-web
  - mkdir -p deploy
  - mv StudyProject-springboot-web.zip deploy/StudyProject-springboot-web.zip

deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY AKIA3CHWVKLTNYMBODNR
    secret_access_key: $AWS_SECRET_KEY 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
    bucket: springboot-webservice-build
    region: ap-northeast-2
    skip_cleanup: true
    acl: private
    local_dir: deploy #
    #wait-until-deployed: true

  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY AKIA3CHWVKLTNYMBODNR
    secret_access_key: $AWS_SECRET_KEY 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
    bucket: springboot-webservice-build # S3 버킷
    key: TestSpring.zip # 빌드 파일을 압축해서 전달
    bundle_type: zip
    application: codedeploy # 웹 콘솔에서 등록한 CodeDeploy 어플리케이션
    deployment_group: StudyProject-springboot-web-group        # 웹 콘솔에서 등록한 CodeDeploy 배포 그룹
    region: ap-northeast-2
    #wait-until-deployed: true 오류가 생긴다면 주석처리

#CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      - ejhjjp11@naver.com
